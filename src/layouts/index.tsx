import React from 'react';
import { Outlet } from 'react-router-dom';

const BasicLayout = () => {
  return (
    <div>
      123213213123123
      <Outlet />
    </div>
  );
};

export default BasicLayout;
